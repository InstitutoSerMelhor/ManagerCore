# Env properties
api.security.token.secret=${JWT_SECRET:superS3cr3t}

# Mongo properties
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=manager_ism_db
spring.data.mongodb.username=root
spring.data.mongodb.password=secret
spring.data.mongodb.uri=seila

# File properties
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB

# Mongock properties
mongock.migration-scan-package=com.institutosermelhor.ManagerCore.infra.config
